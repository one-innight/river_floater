# 河道主动巡护系统（单机运行包）

## 适用环境

Windows 10/11 64 位电脑，建议内存 8 GB 及以上；首次安装依赖时需要联网。

## 首次运行

1. 安装 Miniconda（或 Anaconda），安装时启用 `conda` 命令。
2. 将压缩包解压到纯英文路径，例如 `D:\river-patrol`。
3. 打开 Miniconda Prompt，进入项目目录：

   ```bat
   cd /d D:\river-patrol
   ```

4. 执行环境初始化。首次运行可能需要较长时间下载 Python、CPU 版 PyTorch 和其他依赖：

   ```powershell
   powershell -ExecutionPolicy Bypass -File .\setup_conda_env.ps1
   ```

5. 初始化成功后启动系统：

   ```powershell
   powershell -ExecutionPolicy Bypass -File .\run.ps1
   ```

6. 在本机浏览器打开：<http://127.0.0.1:8000/>

## 登录说明

- 默认账号：`admin`
- 默认密码：`admin123456`

首次登录后，请复制项目根目录的 `.env.example` 并命名为 `.env`，设置自己的 `RIVER_ADMIN_PASSWORD` 与 `RIVER_SESSION_SECRET`，然后重启系统。

## 移动端访问

手机和电脑处于同一局域网时，将 `127.0.0.1` 替换为电脑的局域网 IP，例如：

<http://192.168.1.10:8000/mobile/>

局域网 HTTP 可以使用图片上传；手机浏览器调用摄像头通常要求 HTTPS，请使用公网 HTTPS 地址，或在电脑端使用摄像头。

## 数据与模型

- 当前模型：`weights\river_floater.pt`
- 事件数据库：`river_litter_mvp.db`
- 现场原图、标注图和复核图：`static\uploads\`

升级或迁移前，请备份数据库和上传图片目录。

本包不包含服务器 `.env.server`、SSH 密钥、云账号或 Cloudflare 凭据等敏感信息。
