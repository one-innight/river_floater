# 河道垃圾主动巡护 MVP 需求

## 目标

完成“上传河道图片 → 检测河道垃圾 → 自动预警和建议 → 人工清理 → 图片复核 → 事件关闭”的最小闭环。

## 功能范围

1. 用户可上传图片，或接入浏览器摄像头、USB 摄像头和 RTSP/IP 摄像头。
2. 后端可按固定间隔从 RTSP 视频中自动抽帧，网页关闭后仍继续运行。
3. 单类 YOLO 模型检测图片中的可见垃圾，输出检测框、数量和置信度。
4. 系统根据置信度、数量和覆盖面积生成 0～3 级垃圾风险。
5. 连续两帧发现垃圾创建事件，明显垃圾立即创建；同河段事件不重复创建。
6. 事件按“待确认、已派单、处理中、已处理、复核通过、已关闭”流转。
7. 清理后必须上传复核图片；只有模型未发现垃圾时才能关闭。
8. 系统保存原图、检测框图片、事件状态和处置记录。

## 不在当前范围

- COD、氨氮、总磷等真实水质检测
- 废弃船只、捕鱼和养殖设施识别
- GPS、地图和消息通知
- 多模态传感器、ONNX/TensorRT 和边缘设备部署

## 验收标准

- `/health` 能显示模型可用且类别包含 `Litter`。
- `images/p4.jpg` 能检测出河道垃圾并生成预警。
- `images/p1.jpg` 不应因为藻华生成垃圾事件。
- 摄像头连续两帧发现垃圾时只创建一条未关闭事件。
- RTSP 后台推理不依赖管理网页保持打开。
- 事件能够完成派单、处理、图片复核和关闭。
- 复核图片仍检测到垃圾时，系统必须拒绝进入“复核通过”。
